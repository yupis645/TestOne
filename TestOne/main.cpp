#include <stdio.h>

int main(void)
{
	int A = 0;
	char B;

	printf("これは練習用プログラムである");

	printf("ブランチ作成後に追加した行");

	if (A == 0) {
		printf("Aは0,でもAは1かも？ , Bはどう?");
	}

	printf("新しく追記したぞ。char Bを足した");

	return 0;
}